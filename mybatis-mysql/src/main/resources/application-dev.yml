server:
  port: 9749

spring:
  application:
    name: BootMM
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/limo?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.limo.boot.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug